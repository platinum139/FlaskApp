apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: flaskapp
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: mysql-port
    protocol: TCP
    name: mysql-service-port
